# TODO List

- [ ] Fix failing Angular unit tests due to missing service dependencies. Mock required services using `TestBed.configureTestingModule()` and `spyOn()` where necessary.
- [ ] After the Angular 21 test suite is stable, evaluate an optional migration from Karma to Vitest to align the test setup with newer Angular guidance.
