import { IosZoomDirective } from './ios-zoom.directive';

describe('IosZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new IosZoomDirective();
    expect(directive).toBeTruthy();
  });
});
